diff -r 73d86028534e common/autoconf/generated-configure.sh
--- a/common/autoconf/generated-configure.sh	Mon Jan 10 00:14:03 2022 +0000
+++ b/common/autoconf/generated-configure.sh	Tue Nov 15 01:31:19 2022 +0300
@@ -48024,6 +48024,10 @@
       fi
     fi
 
+    if test "x$SYSROOT" != x; then
+      FREETYPE_CFLAGS="$FREETYPE_CFLAGS --sysroot $SYSROOT"
+    fi
+
     if test "x$FREETYPE_LIBS" = x; then
 
   if test "x$OPENJDK_BUILD_OS_ENV" = "xwindows.cygwin"; then
diff -r 73d86028534e common/autoconf/libraries.m4
--- a/common/autoconf/libraries.m4	Mon Jan 10 00:14:03 2022 +0000
+++ b/common/autoconf/libraries.m4	Tue Nov 15 01:31:19 2022 +0300
@@ -578,6 +578,10 @@
       fi
     fi
 
+    if test "x$SYSROOT" != x; then
+      FREETYPE_CFLAGS="$FREETYPE_CFLAGS --sysroot $SYSROOT"
+    fi
+
     if test "x$FREETYPE_LIBS" = x; then
       BASIC_FIXUP_PATH(FREETYPE_LIB_PATH)
       if test "x$OPENJDK_TARGET_OS" = xwindows; then
